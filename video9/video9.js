console.log('Hello world from HTMl');

let a = {
    name: 'Hoan',
    age: 25
};
// Arrays

let b = ['Mu', 'Real', 'Baccar'];

console.log('typeof b: ', typeof b, b);

// Video10, == vs === , empty, null, undefined

// === : type, value;
// ==  : value;

// Should use === 